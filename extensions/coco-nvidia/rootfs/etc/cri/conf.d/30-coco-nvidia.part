# =============================================================================
# Containerd CRI drop-in — NVIDIA GPU add-on for Confidential Containers
# =============================================================================
# Placed at /etc/cri/conf.d/30-coco-nvidia.part by the coco-kata-nvidia-gpu
# system extension. Talos merges all .part files in /etc/cri/conf.d/ in
# lexical order — this file extends (does not replace) the base extension's
# 20-coco.part.
#
# Registered runtime handler:
#   - kata-qemu-nvidia-gpu-snp : AMD SEV-SNP confidential VM with NVIDIA GPU
#                                VFIO passthrough (driver + NVRC in-guest)
#
# Requires on the same node:
#   - base extension coco-kata-containers from the SAME release (same
#     version, same Kata — provides virtiofsd, AMDSEV.fd OVMF firmware, and
#     containerd-shim-kata-v2). This add-on ships its own SNP-experimental
#     QEMU as of 1.5.0.
# =============================================================================

[plugins."io.containerd.cri.v1.runtime".containerd.runtimes.kata-qemu-nvidia-gpu-snp]
  runtime_type = "io.containerd.kata.v2"
  privileged_without_host_devices = true
  pod_annotations = ["io.katacontainers.*"]
[plugins."io.containerd.cri.v1.runtime".containerd.runtimes.kata-qemu-nvidia-gpu-snp.options]
  ConfigPath = "/usr/local/share/kata-containers/configuration-qemu-nvidia-gpu-snp.toml"
